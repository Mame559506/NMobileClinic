{
  "name": "nancymobile",
  "version": "1.0.1",
  "scripts": {
    "build": "cd backend/src && npm install && cd ../../frontend && npm install && npm run build",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}